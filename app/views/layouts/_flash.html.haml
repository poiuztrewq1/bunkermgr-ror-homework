- if flash.any?
  #flash
    - flash.each do |type,msg|
      .alert.alert-success.alert-dismissible.fade.show{:class => (type == 'notice'? 'alert-success' : 'alert-warning'),:role => "alert"}
        = msg
        %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
          %span{"aria-hidden" => "true"} ×
